/**
 * Central waitlist config.
 * When the Skool community is ready, update COMMUNITY_URL here
 * and flip USE_WAITLIST to false — all CTAs update instantly.
 */
export const USE_WAITLIST = true;
export const COMMUNITY_URL = "https://www.skool.com/ai-integrity"; // update when live
export const CTA_LABEL = USE_WAITLIST ? "Join the founding cohort" : "Join the community — free";
